A network architecture defines how a network is designed and how devices communicate with each other. A network architecture is made using:

- Network topology
- Cable type
- Access method

Before learning Ethernet architectures, two communication methods are important.
# Baseband

Baseband communication sends digital signals through a single channel and uses the entire bandwidth of the cable. The signal is transmitted as electrical pulses or light signals depending on the cable type.

Baseband is bidirectional, which means the same channel can be used for both sending and receiving data.

### Important Points

- Uses digital signals
- Single communication channel
- Uses full bandwidth
- Bidirectional communication
- Commonly used in Ethernet networks

# Broadband

Broadband communication sends data as analog signals using electromagnetic or optical waves. The bandwidth is divided into multiple channels, so multiple transmissions can happen at the same time.

Broadband is unidirectional, so separate paths or frequencies are needed for sending and receiving data.

### Important Points

- Uses analog signals
- Multiple channels/frequencies
- Multiple transmissions at the same time
- Unidirectional communication
- Uses separate send and receive paths

# Ethernet

Ethernet is the most popular network architecture used in LANs and follows the IEEE 802.3 standard. Ethernet networks mainly use:

- Baseband communication
- CSMA/CD access method

In Ethernet naming:

- Number = speed of network
- Base = baseband transmission
- Last letter = cable type

Example:  
10BaseT means:

- 10 Mbps speed
- Baseband communication
- Twisted-pair cable

# 10Base2

10Base2 is an Ethernet architecture that runs at 10 Mbps and uses thinnet coaxial cable (RG-58). It mainly uses a bus topology.

The “2” in the name represents approximately 200 meters cable distance (actual distance is 185 meters).

### Important Points

- Speed: 10 Mbps
- Cable: Thinnet (RG-58)
- Topology: Bus
- Access Method: CSMA/CD
- Max distance: 185 meters per segment
- Maximum 30 hosts per segment
- Minimum distance between hosts: 0.5 meters

# 10Base5

10Base5 also runs at 10 Mbps but uses thicknet coaxial cable (RG-8). Thicknet cable is thicker and allows signals to travel longer distances than thinnet.

The “5” represents approximately 500 meters cable distance.

### Important Points

- Speed: 10 Mbps
- Cable: Thicknet (RG-8)
- Topology: Bus
- Access Method: CSMA/CD
- Max distance: 500 meters per segment
- Maximum 100 hosts per segment
- Minimum distance between hosts: 2.5 meters

# 10BaseT

10BaseT uses twisted-pair cable and a star topology. All devices connect to a central hub or switch.

It mainly uses CAT 3 UTP cable, but higher cables like CAT 5 can also work because they are backward compatible.

### Important Points

- Speed: 10 Mbps
- Cable: CAT 3 UTP
- Topology: Star
- Access Method: CSMA/CD
- Uses RJ-45 connector
- Uses hub or switch

# 10BaseFL

10BaseFL is a fiber-optic Ethernet architecture. It is mainly used as a backbone network for longer distances.

### Important Points

- Speed: 10 Mbps
- Cable: Fiber-optic
- Used for long-distance communication
- Commonly used as backbone connection

# Fast Ethernet (100BaseTX and 100BaseFX)

100BaseTX and 100BaseFX are part of the 100BaseX family, known as Fast Ethernet. Both architectures run at 100 Mbps, use a star topology, and use CSMA/CD as the access method.

The main difference between them is the type of cable used.

## 100BaseTX

100BaseTX uses CAT 5 twisted-pair cable. It uses two wire pairs (4 wires) for communication.

### Important Points

- Speed: 100 Mbps
- Cable: CAT 5 UTP
- Uses 4 wires
- Topology: Star
- Access Method: CSMA/CD

## 100BaseFX

100BaseFX uses fiber-optic cable instead of twisted-pair cable. It uses two fiber strands for communication.

### Important Points

- Speed: 100 Mbps
- Cable: Fiber-optic
- Uses 2 fiber strands
- Topology: Star
- Access Method: CSMA/CD
- Better for longer distances

# IEEE 802.3z

IEEE 802.3z defines Gigabit Ethernet that runs at 1000 Mbps (1 Gbps) using fiber-optic or coaxial cable.

## 1000BaseSX

1000BaseSX uses multimode fiber (MMF) cable and is designed for short distances.

### Important Points

- Speed: 1 Gbps
- Cable: Multimode Fiber (MMF)
- Max distance: 550 meters
- Used for short-distance communication

## 1000BaseLX

1000BaseLX uses single-mode fiber (SMF) cable and supports longer distances.

### Important Points

- Speed: 1 Gbps
- Cable: Single-Mode Fiber (SMF)
- Max distance: 3 kilometers
- Used for long-distance communication

## 1000BaseCX

1000BaseCX uses coaxial cable for communication.

### Important Points

- Speed: 1 Gbps
- Cable: Coaxial cable
- Max distance: 25 meters

# IEEE 802.3ab (1000BaseTX)

IEEE 802.3ab defines 1000BaseTX Gigabit Ethernet using twisted-pair cable.

It uses:

- CAT 5e or CAT 6 UTP cable
- RJ-45 connector
- CSMA/CD access method

Unlike 100BaseTX which uses 4 wires, 1000BaseTX uses all 8 wires (4 pairs).

### Important Points

- Speed: 1 Gbps
- Cable: CAT 5e or CAT 6
- Uses RJ-45 connector
- Uses all 8 wires
- Access Method: CSMA/CD

# 10-Gigabit Ethernet

10-Gigabit Ethernet runs at 10 Gbps (10,000 Mbps) and mainly uses fiber-optic cable.

## 10GBaseSR

Uses short-range multimode fiber.

### Important Points

- Speed: 10 Gbps
- Cable: MMF
- Max distance: 100 meters

## 10GBaseLR

Uses long-range single-mode fiber.

### Important Points

- Speed: 10 Gbps
- Cable: SMF
- Max distance: 10 kilometers

## 10GBaseER

Uses extra-long-range single-mode fiber.

### Important Points

- Speed: 10 Gbps
- Cable: SMF
- Max distance: 40 kilometers

## 10GBaseT

Uses CAT 6 UTP cable.

### Important Points

- Speed: 10 Gbps
- Cable: CAT 6 UTP
- Max distance: 100 meters

# WAN Versions of 10-Gigabit Ethernet

These standards connect to SONET networks using fiber-optic cable.

## 10GBaseSW

- Uses multimode fiber
- Max distance: 100 meters

## 10GBaseLW

- Uses single-mode fiber
- Max distance: 10 kilometers

## 10GBaseEW

- Uses single-mode fiber
- Max distance: 40 kilometers

---
# Unraveling the Ethernet Name Jargon

Ethernet names follow the format:

##BaseXX

Each part of the name has a meaning.

## Speed of Network

The numbers at the beginning show the speed of the network.

Examples:

- 10 = 10 Mbps
- 100 = 100 Mbps
- 1000 = 1000 Mbps (1 Gbps)
- 10G = 10 Gbps

## Base

“Base” means baseband transmission is used.

## Cable Type / Medium

The last part shows the cable type or medium used.

### 5

- Represents Thicknet cable
- Maximum distance: 500 meters

Example:

- 10Base5

### 2

- Represents Thinnet cable
- Maximum distance: about 200 meters (185 m actual)

Example:

- 10Base2

### T

- Represents Twisted-Pair cable

Example:

- 10BaseT

Sometimes T also shows wire pairs:

- T4 = 4 wire pairs used

### F

- Represents Fiber-optic cable

Example:

- 10BaseF

### X

- Represents higher-speed connection

Examples:

- 100BaseTX
- 100BaseFX

# Example: 100BaseTX

100BaseTX means:

- 100 = 100 Mbps speed
- Base = Baseband transmission
- TX = Twisted-pair cable

Since speed is 100 Mbps, it usually uses at least CAT 5 cable.